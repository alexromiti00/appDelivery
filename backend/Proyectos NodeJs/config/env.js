/**
 * Un objeto que contiene la configuración del entorno de ejecución del código.
 * @typedef {Object} EnvironmentConfig
 * @property {boolean} production - Indica si el código está en modo producción o no.
 * @property {number} id_developer - El ID del desarrollador que está trabajando en el código.
 */

/**
 * Exporta un objeto con la configuración del entorno de ejecución del código.
 * @type {EnvironmentConfig}
 */

module.exports = {
    /**
     * Indica si el código está en modo producción o no.
     * @type {boolean}
     */
    production: false,
    /**
     * El ID del desarrollador que está trabajando en el código.
     * @type {number}
     */

    id_developer: 0
}